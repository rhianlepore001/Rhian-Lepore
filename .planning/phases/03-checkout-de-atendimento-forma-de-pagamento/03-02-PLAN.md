---
phase: 03-checkout-de-atendimento-forma-de-pagamento
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - components/CheckoutModal.test.tsx
  - components/CheckoutModal.tsx
autonomous: true
requirements:
  - REQ-020
  - REQ-021
  - REQ-022
  - REQ-023

must_haves:
  truths:
    - "CheckoutModal bloqueia confirmação se payment_method não foi selecionado — campo fica vermelho com mensagem"
    - "Campo taxa de maquininha aparece apenas quando Débito ou Crédito selecionado"
    - "Dropdown 'Recebido por' lista membros ativos da empresa + entrada fixa 'Dono'"
    - "Botão 'Confirmar Pagamento' exibe loading state durante a operação"
    - "Modal fecha e limpa estado ao fechar sem confirmar (sem re-abrir com valores anteriores)"
    - "Ao confirmar: UPDATE campos de pagamento em appointments → RPC complete_appointment → onConfirm()"
  artifacts:
    - path: "components/CheckoutModal.tsx"
      provides: "Modal de conclusão de atendimento"
      exports: ["CheckoutModal", "CheckoutModalProps"]
      min_lines: 120
    - path: "components/CheckoutModal.test.tsx"
      provides: "Testes do CheckoutModal"
      contains: "bloqueia confirmação sem payment_method"
  key_links:
    - from: "CheckoutModal"
      to: "supabase.from('appointments').update()"
      via: "UPDATE com received_by, machine_fee_applied, machine_fee_percent, price"
      pattern: "supabase.*appointments.*update"
    - from: "CheckoutModal"
      to: "supabase.rpc('complete_appointment')"
      via: "chamada após UPDATE bem-sucedido"
      pattern: "rpc.*complete_appointment"
    - from: "CheckoutModal"
      to: "business_settings"
      via: "busca taxas com .eq('user_id', companyId)"
      pattern: "business_settings.*companyId"
---

<objective>
Criar o componente `CheckoutModal.tsx` completo com todos os campos definidos em D-02, e o arquivo de testes `CheckoutModal.test.tsx` cobrindo os comportamentos críticos (REQ-020 a REQ-023).

Purpose: Entrega o coração do fluxo de checkout — o modal que captura forma de pagamento, taxa e recebedor antes de concluir o atendimento.
Output: CheckoutModal pronto para importação em Agenda.tsx (Plano 04).
</objective>

<execution_context>
@C:/Users/User/Downloads/Rhian-Lepore-main/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/Downloads/Rhian-Lepore-main/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-CONTEXT.md
@.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-RESEARCH.md
@.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-01-SUMMARY.md

<interfaces>
<!-- Extraído de components/Modal.tsx -->
```typescript
interface ModalProps {
    isOpen: boolean;
    onClose: () => void;
    title?: string;
    children: React.ReactNode;
    size?: 'sm' | 'md' | 'lg' | 'xl' | 'full';
    showCloseButton?: boolean;
    footer?: React.ReactNode;
    preventClose?: boolean;
    forceTheme?: 'beauty' | 'barber';
}
// Uso: <Modal isOpen={...} onClose={...} title="..." size="md" footer={<ModalFooter>...}>
```

<!-- Extraído de types.ts — interface Appointment atualizada no Plano 01 -->
```typescript
export interface Appointment {
  id: string;
  clientName: string;
  client_id?: string;
  clientPhone?: string;
  service: string;
  time: string;
  appointment_time: string;
  status: 'Confirmed' | 'Pending' | 'Completed';
  price: number;
  basePrice?: number;
  professional_id?: string;
  notes?: string;
  user_id?: string;
  payment_method?: string;
  received_by?: string | null;
  machine_fee_applied?: boolean;
  machine_fee_percent?: number | null;
}
```

<!-- Padrão de auth — contexts/AuthContext.tsx -->
```typescript
// useAuth() retorna: { user, companyId, role, userType, businessName, teamMemberId }
// companyId: sempre usar para filtros multi-tenant (nunca user.id para settings do dono)
```

<!-- Padrão de team_members — CommissionsSettings.tsx:43-50 -->
```typescript
const { data: teamMembers } = await supabase
  .from('team_members')
  .select('id, name, active')
  .eq('user_id', companyId)   // companyId, não user.id
  .eq('active', true)
  .order('name');
```

<!-- Padrão de busca de settings financeiras -->
```typescript
const { data: settings } = await supabase
  .from('business_settings')
  .select('machine_fee_enabled, debit_fee_percent, credit_fee_percent')
  .eq('user_id', companyId)   // sempre companyId
  .maybeSingle();
```

<!-- Padrão de alertas -->
```typescript
const { showAlert } = useAlerts();
showAlert({ type: 'error', message: 'Erro ao concluir atendimento. Tente novamente.' });
```

<!-- BrutalButton -->
```typescript
<BrutalButton variant="primary" onClick={fn} loading={loading} disabled={loading}>
  Confirmar Pagamento
</BrutalButton>
// variants: 'primary' | 'ghost' | 'danger'
// loading prop: exibe spinner integrado e desabilita automaticamente
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar scaffolding de testes CheckoutModal.test.tsx</name>
  <files>components/CheckoutModal.test.tsx</files>
  <read_first>
    - test/setup.ts (verificar mocks de Supabase disponíveis)
    - components/AppointmentEditModal.tsx (ver padrão de testes de modal existente)
    - vitest.config.ts (confirmar configuração de test environment)
  </read_first>
  <behavior>
    - Teste 1: renderiza sem crash com appointment válido
    - Teste 2: botão "Confirmar Pagamento" desabilitado e campo payment_method com classe de erro ao submeter sem selecionar método
    - Teste 3: campo taxa de maquininha NÃO aparece quando PIX selecionado
    - Teste 4: campo taxa de maquininha NÃO aparece quando Dinheiro selecionado
    - Teste 5: campo taxa de maquininha APARECE quando Débito selecionado
    - Teste 6: campo taxa de maquininha APARECE quando Crédito selecionado
    - Teste 7: dropdown "Recebido por" contém entrada "Dono" como primeira opção
    - Teste 8: dropdown "Recebido por" lista team members passados via prop
  </behavior>
  <action>
Criar `components/CheckoutModal.test.tsx` com os testes definidos em `<behavior>`. Usar `vi.mock('@/lib/supabase')` e `vi.mock('@/contexts/AuthContext')`. Os testes devem FALHAR inicialmente (CheckoutModal.tsx ainda não existe) — este é o comportamento esperado em TDD.

Estrutura base do arquivo de testes:

```typescript
import { render, screen, fireEvent } from '@testing-library/react';
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { CheckoutModal } from './CheckoutModal';
import type { Appointment } from '@/types';

vi.mock('@/lib/supabase', () => ({
  supabase: {
    from: vi.fn().mockReturnThis(),
    select: vi.fn().mockReturnThis(),
    update: vi.fn().mockReturnThis(),
    eq: vi.fn().mockReturnThis(),
    maybeSingle: vi.fn().mockResolvedValue({ data: null, error: null }),
    rpc: vi.fn().mockResolvedValue({ data: null, error: null }),
  }
}));

vi.mock('@/contexts/AuthContext', () => ({
  useAuth: () => ({
    user: { id: 'owner-123' },
    companyId: 'company-abc',
    role: 'owner',
    userType: 'barber',
  })
}));

vi.mock('@/contexts/AlertsContext', () => ({
  useAlerts: () => ({ showAlert: vi.fn() })
}));

const mockAppointment: Appointment = {
  id: 'apt-001',
  clientName: 'João Silva',
  service: 'Corte Masculino',
  time: '10:00',
  appointment_time: '2026-04-13T10:00:00',
  status: 'Confirmed',
  price: 40,
};

const mockTeamMembers = [
  { id: 'tm-001', name: 'Carlos', active: true },
  { id: 'tm-002', name: 'Ana', active: true },
];

describe('CheckoutModal', () => {
  const defaultProps = {
    appointment: mockAppointment,
    teamMembers: mockTeamMembers,
    financialSettings: null,
    onClose: vi.fn(),
    onConfirm: vi.fn(),
  };

  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('renderiza sem crash com appointment válido', () => {
    render(<CheckoutModal {...defaultProps} />);
    expect(screen.getByText('Concluir Atendimento')).toBeInTheDocument();
  });

  it('exibe mensagem de erro e campo vermelho ao confirmar sem payment_method', () => {
    render(<CheckoutModal {...defaultProps} />);
    fireEvent.click(screen.getByText('Confirmar Pagamento'));
    expect(screen.getByText('Selecione a forma de pagamento')).toBeInTheDocument();
  });

  it('NÃO exibe campo taxa quando PIX selecionado', () => {
    render(<CheckoutModal {...defaultProps} />);
    fireEvent.click(screen.getByLabelText('PIX'));
    expect(screen.queryByLabelText(/taxa/i)).not.toBeInTheDocument();
  });

  it('NÃO exibe campo taxa quando Dinheiro selecionado', () => {
    render(<CheckoutModal {...defaultProps} />);
    fireEvent.click(screen.getByLabelText('Dinheiro'));
    expect(screen.queryByLabelText(/taxa/i)).not.toBeInTheDocument();
  });

  it('EXIBE campo taxa quando Débito selecionado', () => {
    render(<CheckoutModal {...defaultProps} />);
    fireEvent.click(screen.getByLabelText('Débito'));
    expect(screen.getByLabelText(/taxa de maquininha/i)).toBeInTheDocument();
  });

  it('EXIBE campo taxa quando Crédito selecionado', () => {
    render(<CheckoutModal {...defaultProps} />);
    fireEvent.click(screen.getByLabelText('Crédito'));
    expect(screen.getByLabelText(/taxa de maquininha/i)).toBeInTheDocument();
  });

  it('dropdown Recebido por contém opção Dono como primeira entrada', () => {
    render(<CheckoutModal {...defaultProps} />);
    const select = screen.getByLabelText(/recebido por/i);
    const options = Array.from(select.querySelectorAll('option'));
    expect(options[1].textContent).toBe('Dono');
  });

  it('dropdown Recebido por lista team members passados via prop', () => {
    render(<CheckoutModal {...defaultProps} />);
    expect(screen.getByText('Carlos')).toBeInTheDocument();
    expect(screen.getByText('Ana')).toBeInTheDocument();
  });
});
```
  </action>
  <verify>
    <automated>npm test -- --run --reporter=verbose components/CheckoutModal.test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `components/CheckoutModal.test.tsx` existe
    - `npm test -- --run components/CheckoutModal.test` executa e reporta 8 testes (todos falhando inicialmente, pois CheckoutModal.tsx não existe ainda)
    - Após Tarefa 2 ser concluída, os 8 testes devem passar
  </acceptance_criteria>
  <done>Arquivo de testes criado com 8 casos de teste cobrindo REQ-020 a REQ-023. Testes prontos para guiar a implementação.</done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Implementar CheckoutModal.tsx</name>
  <files>components/CheckoutModal.tsx</files>
  <read_first>
    - components/Modal.tsx (interface e uso completo — ler ANTES de criar o componente)
    - components/BrutalButton.tsx (props disponíveis — loading, variant, disabled)
    - components/CheckoutModal.test.tsx (testes criados na Tarefa 1 — guiam a implementação)
    - contexts/AuthContext.tsx (exports de useAuth — user, companyId, role)
    - utils/formatters.ts (verificar se formatCurrency existe e assinatura)
    - utils/Logger.ts (verificar export de logger)
  </read_first>
  <behavior>
    - Renderiza modal com title "Concluir Atendimento" usando componente Modal
    - Exibe serviço (read-only) e valor (editável) pré-preenchidos do appointment
    - Radio buttons para PIX, Dinheiro, Débito, Crédito (obrigatório — per D-02)
    - Campo taxa de maquininha aparece SOMENTE para débito/crédito, pré-preenchido de financialSettings
    - Dropdown "Recebido por" com opção vazia + "Dono" (fixo) + team members ativos
    - Validação: bloqueia confirmação se payment_method vazio, mostra "Selecione a forma de pagamento" em vermelho
    - Loading state no botão durante operação
    - Sequência de confirmação: UPDATE appointments → RPC complete_appointment → onConfirm()
    - Reset de estado quando appointment muda para null (per Pitfall 4)
  </behavior>
  <action>
Criar `components/CheckoutModal.tsx` para que os 8 testes da Tarefa 1 passem. Implementação completa:

```typescript
import React, { useState, useEffect } from 'react';
import { Modal } from '@/components/Modal';
import { BrutalButton } from '@/components/BrutalButton';
import { useAuth } from '@/contexts/AuthContext';
import { useAlerts } from '@/contexts/AlertsContext';
import { supabase } from '@/lib/supabase';
import { logger } from '@/utils/Logger';
import type { Appointment } from '@/types';

interface TeamMember {
  id: string;
  name: string;
  active: boolean;
}

interface FinancialSettings {
  machine_fee_enabled: boolean;
  debit_fee_percent: number;
  credit_fee_percent: number;
}

export interface CheckoutModalProps {
  appointment: Appointment | null;
  teamMembers: TeamMember[];
  financialSettings: FinancialSettings | null;
  onClose: () => void;
  onConfirm: () => void;
}

export const CheckoutModal: React.FC<CheckoutModalProps> = ({
  appointment,
  teamMembers,
  financialSettings,
  onClose,
  onConfirm,
}) => {
  const { companyId } = useAuth();
  const { showAlert } = useAlerts();

  const [paymentMethod, setPaymentMethod] = useState<string>('');
  const [receivedBy, setReceivedBy] = useState<string>('');
  const [machineFeePercent, setMachineFeePercent] = useState<string>('');
  const [finalPrice, setFinalPrice] = useState<number>(0);
  const [loading, setLoading] = useState(false);
  const [errors, setErrors] = useState<{ paymentMethod?: string }>({});

  // Reset estado quando appointment muda (D-Pitfall 4)
  useEffect(() => {
    if (appointment) {
      setPaymentMethod('');
      setReceivedBy('');
      setMachineFeePercent('');
      setFinalPrice(appointment.price ?? 0);
      setErrors({});
      setLoading(false);
    }
  }, [appointment?.id]);

  // Pré-preencher taxa quando método muda (per D-02 e RESEARCH Pattern 2)
  useEffect(() => {
    if (paymentMethod === 'debit' && financialSettings?.debit_fee_percent) {
      setMachineFeePercent(String(financialSettings.debit_fee_percent));
    } else if (paymentMethod === 'credit' && financialSettings?.credit_fee_percent) {
      setMachineFeePercent(String(financialSettings.credit_fee_percent));
    } else if (!['debit', 'credit'].includes(paymentMethod)) {
      setMachineFeePercent('');
    }
  }, [paymentMethod, financialSettings]);

  const showMachineFee = ['debit', 'credit'].includes(paymentMethod);

  const handleConfirm = async () => {
    if (!paymentMethod) {
      setErrors({ paymentMethod: 'Selecione a forma de pagamento' });
      return;
    }
    if (!appointment) return;

    setLoading(true);
    setErrors({});

    try {
      // 1. UPDATE dos campos de checkout (per RESEARCH Pattern 2)
      const { error: updateError } = await supabase
        .from('appointments')
        .update({
          payment_method: paymentMethod,
          received_by: receivedBy || null,
          machine_fee_applied: showMachineFee && !!machineFeePercent,
          machine_fee_percent: showMachineFee && machineFeePercent
            ? parseFloat(machineFeePercent)
            : null,
          price: finalPrice,
        })
        .eq('id', appointment.id);

      if (updateError) throw updateError;

      // 2. RPC complete_appointment para mudar status e criar finance_record
      const { error: rpcError } = await supabase
        .rpc('complete_appointment', { p_appointment_id: appointment.id });

      if (rpcError) throw rpcError;

      onConfirm();
    } catch (err: unknown) {
      const errorMessage = err instanceof Error ? err.message : 'Erro desconhecido';
      logger.error('Erro ao concluir atendimento via checkout', { error: errorMessage });
      showAlert({
        type: 'error',
        message: 'Erro ao concluir atendimento. Tente novamente.',
      });
    } finally {
      setLoading(false);
    }
  };

  const paymentMethods = [
    { value: 'pix', label: 'PIX' },
    { value: 'cash', label: 'Dinheiro' },
    { value: 'debit', label: 'Débito' },
    { value: 'credit', label: 'Crédito' },
  ];

  return (
    <Modal
      isOpen={!!appointment}
      onClose={onClose}
      title="Concluir Atendimento"
      size="md"
      preventClose={loading}
      footer={
        <div className="flex items-center justify-between w-full">
          <BrutalButton variant="ghost" onClick={onClose} disabled={loading}>
            Cancelar
          </BrutalButton>
          <BrutalButton variant="primary" onClick={handleConfirm} loading={loading}>
            Confirmar Pagamento
          </BrutalButton>
        </div>
      }
    >
      <div className="space-y-4">
        {/* Serviço (read-only) */}
        <div>
          <label className="block text-xs font-mono uppercase text-neutral-400 mb-1">
            Serviço
          </label>
          <p className="text-white font-medium">{appointment?.service}</p>
        </div>

        {/* Valor (editável) */}
        <div>
          <label htmlFor="checkout-price" className="block text-xs font-mono uppercase text-neutral-400 mb-1">
            Valor (R$)
          </label>
          <input
            id="checkout-price"
            type="number"
            step="0.01"
            min="0"
            value={finalPrice}
            onChange={(e) => setFinalPrice(parseFloat(e.target.value) || 0)}
            className="w-full bg-neutral-800 border border-neutral-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-accent-gold"
          />
        </div>

        {/* Forma de Pagamento (obrigatório — D-02) */}
        <div>
          <p className="block text-xs font-mono uppercase text-neutral-400 mb-2">
            Forma de Pagamento <span className="text-red-500">*</span>
          </p>
          <div className="grid grid-cols-2 gap-2">
            {paymentMethods.map(({ value, label }) => (
              <label
                key={value}
                className={`flex items-center gap-2 cursor-pointer rounded-lg px-3 py-2 border transition-colors ${
                  paymentMethod === value
                    ? 'border-accent-gold bg-accent-gold/10 text-accent-gold'
                    : 'border-neutral-700 text-neutral-300 hover:border-neutral-500'
                } ${errors.paymentMethod ? 'border-red-500' : ''}`}
              >
                <input
                  type="radio"
                  name="payment-method"
                  value={value}
                  aria-label={label}
                  checked={paymentMethod === value}
                  onChange={() => {
                    setPaymentMethod(value);
                    setErrors({});
                  }}
                  className="sr-only"
                />
                {label}
              </label>
            ))}
          </div>
          {errors.paymentMethod && (
            <p className="text-red-500 text-xs mt-1">{errors.paymentMethod}</p>
          )}
        </div>

        {/* Taxa de Maquininha — apenas para débito/crédito (D-02) */}
        {showMachineFee && (
          <div>
            <label htmlFor="checkout-fee" className="block text-xs font-mono uppercase text-neutral-400 mb-1">
              Taxa de Maquininha (%)
            </label>
            <input
              id="checkout-fee"
              type="number"
              step="0.01"
              min="0"
              max="100"
              placeholder="Ex: 2.5"
              value={machineFeePercent}
              onChange={(e) => setMachineFeePercent(e.target.value)}
              className="w-full bg-neutral-800 border border-neutral-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-accent-gold"
            />
          </div>
        )}

        {/* Recebido Por (D-02, D-23) */}
        <div>
          <label htmlFor="checkout-received-by" className="block text-xs font-mono uppercase text-neutral-400 mb-1">
            Recebido Por
          </label>
          <select
            id="checkout-received-by"
            aria-label="Recebido por"
            value={receivedBy}
            onChange={(e) => setReceivedBy(e.target.value)}
            className="w-full bg-neutral-800 border border-neutral-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-accent-gold"
          >
            <option value="">Selecionar...</option>
            <option value="owner">Dono</option>
            {teamMembers.map((member) => (
              <option key={member.id} value={member.id}>
                {member.name}
              </option>
            ))}
          </select>
        </div>
      </div>
    </Modal>
  );
};
```

Após criar o arquivo, executar os testes para confirmar GREEN:
```bash
npm test -- --run --reporter=verbose components/CheckoutModal.test
```
  </action>
  <verify>
    <automated>npm test -- --run --reporter=verbose components/CheckoutModal.test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run components/CheckoutModal.test` reporta 8/8 testes passando
    - `npm run typecheck` passa sem erros em CheckoutModal.tsx
    - `npm run lint` passa sem warnings em CheckoutModal.tsx
    - Arquivo exporta `CheckoutModal` e `CheckoutModalProps`
    - `grep "received_by: receivedBy" components/CheckoutModal.tsx` retorna resultado
    - `grep "complete_appointment" components/CheckoutModal.tsx` retorna resultado
    - `grep "Selecione a forma de pagamento" components/CheckoutModal.tsx` retorna resultado
  </acceptance_criteria>
  <done>CheckoutModal implementado com todos os campos de D-02, validações de D-07, e 8/8 testes passando.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| usuário (staff/owner) → CheckoutModal | Input de payment_method, machineFeePercent, receivedBy via formulário — cruza para banco via Supabase client |
| CheckoutModal → appointments UPDATE | Dados de pagamento enviados ao banco; RLS valida company_id antes de persistir |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | machine_fee_percent input | mitigate | `parseFloat()` antes de persistir; `DECIMAL(5,2)` no banco rejeita valores inválidos |
| T-03-05 | Tampering | received_by dropdown | mitigate | Valor enviado é UUID de team_members ou null; `ON DELETE SET NULL` FK previne IDs inválidos |
| T-03-06 | Elevation of Privilege | CheckoutModal UPDATE de appointments | accept | RLS `"Appointments: company isolation"` com `get_auth_company_id()` bloqueia cross-tenant — ativo desde Plano 01 |
| T-03-07 | Tampering | Dupla chamada complete_appointment | mitigate | Botão desabilitado durante `loading=true`; RPC age em appointment com status != Completed |
| T-03-08 | Information Disclosure | companyId em queries | accept | `companyId` vem de `useAuth()` (session Supabase) — nunca de URL/form (per CLAUDE.md) |
</threat_model>

<verification>
Após conclusão do plano:
1. `npm test -- --run components/CheckoutModal.test` → 8/8 passando
2. `npm run typecheck` → sem erros
3. `npm run lint` → sem warnings
4. CheckoutModal exportado e pronto para uso em Agenda.tsx
</verification>

<success_criteria>
- 8 testes passando em CheckoutModal.test.tsx
- CheckoutModal.tsx implementado com todos os campos de D-02 e validações de D-07
- Typecheck e lint limpos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-02-SUMMARY.md` com:
- Status dos 8 testes
- Exports criados
- Qualquer ajuste necessário nos testes ou implementação
</output>
