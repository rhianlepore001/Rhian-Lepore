---
id: US-0307
title: Setup Vitest + React Testing Library para testes de componentes
epic: EPIC-003-S1
status: Done
effort: 2h
priority: P1
lead: Uma (@ux-design-expert)
assignee: "@architect"
sprint: 1
week: 2
---

# US-0307: Setup Vitest component testing infrastructure + RTL

**Epic ID:** EPIC-003-S1 (Sprint 1)
**Prioridade:** P1 (Quick Win)
**Esforço:** 2 horas
**Lead:** Uma (@ux-design-expert) / Support: Aria (@architect)
**Objetivo:** Fundação de testes de componentes pronta para expansão em Week 4

---

## 📖 História

**Como um** desenvolvedor de testes,
**Eu quero** ter Vitest + React Testing Library configurado e funcional,
**Para que** possamos escrever e executar testes de componentes com facilidade.

---

## ✅ Critérios de Aceitação

- [x] **AC1:** Vitest instalado e configurado em `vitest.config.ts`
- [x] **AC2:** React Testing Library instalado e setup.ts criado
- [x] **AC3:** Exemplo de test funcionando: `npm test -- Modal.test.tsx` passa
- [x] **AC4:** Coverage report gerado: `npm run test:coverage`
- [x] **AC5:** CI/CD integrado: testes rodam em GitHub Actions

---

## 🤖 CodeRabbit Integration

**Primary Type:** Infrastructure (Testing)
**Complexity:** Medium (2 hours)

**Primary Agents:**
- @architect (Aria) — Testing infrastructure setup
- @ux-design-expert (Uma) — Test strategy guidance

**Quality Gate Tasks:**
- [ ] Pre-Commit: Vitest/RTL config validated
- [ ] Pre-PR: Tests pass locally and in CI

---

## 📋 Tasks / Subtasks

- [ ] **Task 1:** Instalar dependências (AC: 1, 2)
  - [ ] Subtask 1.1: `npm install -D vitest @testing-library/react @testing-library/user-event jsdom`
  - [ ] Subtask 1.2: Atualizar `package.json` com scripts: `"test": "vitest", "test:ui": "vitest --ui", "test:coverage": "vitest --coverage"`
- [ ] **Task 2:** Configurar Vitest (AC: 1)
  - [ ] Subtask 2.1: Atualizar `vitest.config.ts` com jsdom environment
  - [ ] Subtask 2.2: Configurar alias de path (`@/` para root)
  - [ ] Subtask 2.3: Incluir setup file para globals
- [ ] **Task 3:** Criar setup.ts (AC: 2)
  - [ ] Subtask 3.1: Arquivo `test/setup.ts` com imports de testing-library
  - [ ] Subtask 3.2: Configurar custom matchers se necessário
- [ ] **Task 4:** Exemplo de test (AC: 3)
  - [ ] Subtask 4.1: Criar `test/Modal.test.tsx` com teste simples
  - [ ] Subtask 4.2: `npm test` deve passar
- [ ] **Task 5:** Coverage setup (AC: 4)
  - [ ] Subtask 5.1: Configurar coverage em `vitest.config.ts`
  - [ ] Subtask 5.2: `npm run test:coverage` gera HTML report
- [ ] **Task 6:** CI/CD integration (AC: 5)
  - [ ] Subtask 6.1: Adicionar step em `.github/workflows/ci.yml`: `npm test -- --run`
  - [ ] Subtask 6.2: Verificar que testes rodam em CI

---

## 📝 Dev Notes

**Configuração Vitest (vitest.config.ts):**
```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: './test/setup.ts',
  },
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './'),
    },
  },
});
```

**Setup File (test/setup.ts):**
```typescript
import { expect, afterEach, vi } from 'vitest';
import { cleanup } from '@testing-library/react';
import matchers from '@testing-library/jest-dom/matchers';

expect.extend(matchers);

afterEach(() => {
  cleanup();
});
```

**Exemplo Test (test/Modal.test.tsx):**
```typescript
import { render, screen } from '@testing-library/react';
import { Modal } from '@/components/Modal';

describe('Modal', () => {
  it('renders modal title', () => {
    render(<Modal title="Test" isOpen={true}><div>Content</div></Modal>);
    expect(screen.getByText('Test')).toBeInTheDocument();
  });
});
```

**Referenced from:** EPIC-003-S1 Week 2 Task (Testing Infrastructure)

---

## ✅ QA Results

*Preenchido após revisão QA*

---

**Created by:** River (@sm)
**Source:** EPIC-003-S1 Sprint 1 Week 2
**Status:** Ready for Development

