from_agent: ceo
last_command: delegate
target_agent: dev
sub_agent: dev-layout
timestamp: 2026-05-10T15:00:00Z
context: |
  Sprint 1 (Design System) concluído com sucesso.
  Agora iniciando Sprint 2 — Dashboard Redesign.
  
  Decisão: 2 agentes @dev trabalhando em paralelo no Dashboard.
  Este agente foca na ESTRUTURA e SHELL do Dashboard.

task: |
  Redesenhar o SHELL / ESTRUTURA do Dashboard do AgendiX.
  
  Foco: Layout, Grid, Onboarding, Banners, Hero, Empty States, Modais.
  NÃO tocar em: ProfitMetrics, BusinessHealthCard, ActionCenter, MeuDiaWidget (outro agente cuida).
  
  Arquivos:
  - pages/Dashboard.tsx — refatorar grid mobile-first, lazy loading, imports
  - components/dashboard/DashboardHero.tsx — saudação premium, tipografia refinada
  - components/dashboard/SetupCopilot.tsx — tornar colapsável ou tour contextual (não intrusivo)
  - components/EmptyState.tsx — CRIAR componente reutilizável (ícone + mensagem + CTA)
  - Banners no Dashboard.tsx (SmartNotifications, commission, unfinished) — estilizar com tokens
  - Modais do dashboard (GoalSettingsModal, AllAppointmentsModal, MonthlyProfitModal, GoalHistoryModal) — ajustar visual com tokens do useBrutalTheme
  
  Regras técnicas:
  - Usar skills: impeccable, interface-design, ui-styling
  - Zero hardcode de cor. Tudo via useBrutalTheme tokens.
  - Mobile-first: grid 1 coluna mobile, 2 colunas tablet+, assimétrico desktop
  - Preservar lazy loading dos modais
  - Preservar hooks de dados (useDashboardData)
  - Preservar lógica de negócio (useState, useEffect, handlers)
  
  Validações obrigatórias:
  - npm run typecheck (sem erros)
  - npm run lint (sem erros)
  - npm test (todos passando)
  - Mobile: 390px
  - Light mode: funcional (não quebrar)

consumed: false
