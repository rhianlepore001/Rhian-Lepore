---
phase: 03-checkout-de-atendimento-forma-de-pagamento
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260413_checkout_fields.sql
  - types.ts
autonomous: false
requirements:
  - REQ-020
  - REQ-021
  - REQ-022
  - REQ-023
  - REQ-024

must_haves:
  truths:
    - "Tabela appointments possui colunas received_by (UUID), machine_fee_applied (BOOLEAN), machine_fee_percent (DECIMAL(5,2))"
    - "Tabela business_settings possui colunas machine_fee_enabled (BOOLEAN), debit_fee_percent (DECIMAL(5,2)), credit_fee_percent (DECIMAL(5,2))"
    - "Interface Appointment em types.ts reflete os novos campos"
    - "Schema aplicado no banco Supabase sem erros"
  artifacts:
    - path: "supabase/migrations/20260413_checkout_fields.sql"
      provides: "Colunas de checkout em appointments e business_settings"
      contains: "ADD COLUMN IF NOT EXISTS received_by"
    - path: "types.ts"
      provides: "Tipos TypeScript para novos campos de checkout"
      exports: ["Appointment"]
  key_links:
    - from: "types.ts Appointment interface"
      to: "components/CheckoutModal.tsx (Plano 02)"
      via: "import { Appointment } from '@/types'"
      pattern: "received_by.*string.*null"
---

<objective>
Criar a migration SQL com os novos campos de checkout em `appointments` e `business_settings`, aplicar o schema no Supabase, e atualizar a interface `Appointment` em `types.ts` para refletir os novos campos.

Purpose: Estabelece a base de dados necessária para os planos seguintes — sem essas colunas, o CheckoutModal não pode persistir dados de pagamento.
Output: Migration aplicada, types atualizados.
</objective>

<execution_context>
@C:/Users/User/Downloads/Rhian-Lepore-main/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/Downloads/Rhian-Lepore-main/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-CONTEXT.md
@.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-RESEARCH.md

<interfaces>
<!-- Extraído de types.ts — interface atual de Appointment -->
```typescript
export interface Appointment {
  id: string;
  clientName: string;
  client_id?: string;
  clientPhone?: string;
  service: string;
  time: string;
  appointment_time: string;
  status: 'Confirmed' | 'Pending' | 'Completed';
  price: number;
  basePrice?: number;
  professional_id?: string;
  notes?: string;
  user_id?: string;
  // payment_method?: string  já existe (migration 20260218_add_payment_method.sql)
}
```

<!-- Últimas migrations relevantes para contexto de schema -->
<!-- 20260218_add_payment_method.sql: payment_method TEXT já existe em appointments -->
<!-- 20260406_add_mbway_payment_method.sql: get_finance_stats usa payment_method -->
<!-- 20260307_us015b_multi_user_rls.sql: policy "Appointments: company isolation" FOR ALL — cobre UPDATE -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Criar migration SQL com novos campos de checkout</name>
  <files>supabase/migrations/20260413_checkout_fields.sql</files>
  <read_first>
    - supabase/migrations/20260218_add_payment_method.sql (confirmar que payment_method já existe)
    - supabase/migrations/20260307_us015b_multi_user_rls.sql (confirmar policy "Appointments: company isolation" FOR ALL)
    - supabase/migrations/20260218_full_schema_fix.sql (confirmar estrutura de business_settings)
  </read_first>
  <action>
Criar o arquivo `supabase/migrations/20260413_checkout_fields.sql` com o seguinte conteúdo exato:

```sql
-- Fase 3: Checkout de Atendimento + Forma de Pagamento
-- Adiciona campos de checkout em appointments e configurações de taxa em business_settings
-- D-04 e D-05 do CONTEXT.md

-- ============================================================
-- appointments: novos campos de checkout
-- NOTA: payment_method TEXT já existe (20260218_add_payment_method.sql)
-- ============================================================
ALTER TABLE public.appointments
  ADD COLUMN IF NOT EXISTS received_by UUID REFERENCES public.team_members(id) ON DELETE SET NULL,
  ADD COLUMN IF NOT EXISTS machine_fee_applied BOOLEAN DEFAULT FALSE,
  ADD COLUMN IF NOT EXISTS machine_fee_percent DECIMAL(5,2);

-- ============================================================
-- business_settings: configurações de taxa de maquininha
-- Padrão upsert com onConflict: 'user_id' já está em uso
-- ============================================================
ALTER TABLE public.business_settings
  ADD COLUMN IF NOT EXISTS machine_fee_enabled BOOLEAN DEFAULT FALSE,
  ADD COLUMN IF NOT EXISTS debit_fee_percent DECIMAL(5,2) DEFAULT 0,
  ADD COLUMN IF NOT EXISTS credit_fee_percent DECIMAL(5,2) DEFAULT 0;

-- ============================================================
-- RLS: NÃO é necessário criar nova policy
-- A policy "Appointments: company isolation" em 20260307_us015b_multi_user_rls.sql
-- usa FOR ALL com get_auth_company_id() — já cobre UPDATE de received_by e machine_fee_*
-- A policy de UPDATE em business_settings já restringe a get_auth_role() = 'owner'
-- ============================================================
```

Salvar como `supabase/migrations/20260413_checkout_fields.sql`.
  </action>
  <verify>
    <automated>grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/20260413_checkout_fields.sql</automated>
  </verify>
  <acceptance_criteria>
    - `grep "received_by UUID REFERENCES public.team_members" supabase/migrations/20260413_checkout_fields.sql` retorna resultado
    - `grep "machine_fee_applied BOOLEAN DEFAULT FALSE" supabase/migrations/20260413_checkout_fields.sql` retorna resultado
    - `grep "machine_fee_percent DECIMAL(5,2)" supabase/migrations/20260413_checkout_fields.sql` retorna resultado
    - `grep "machine_fee_enabled BOOLEAN DEFAULT FALSE" supabase/migrations/20260413_checkout_fields.sql` retorna resultado
    - `grep "debit_fee_percent DECIMAL(5,2) DEFAULT 0" supabase/migrations/20260413_checkout_fields.sql` retorna resultado
    - `grep "credit_fee_percent DECIMAL(5,2) DEFAULT 0" supabase/migrations/20260413_checkout_fields.sql` retorna resultado
    - Arquivo tem exatamente 6 linhas `ADD COLUMN IF NOT EXISTS`
  </acceptance_criteria>
  <done>Migration criada com 3 colunas em appointments e 3 colunas em business_settings, todas com IF NOT EXISTS e valores default corretos.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Atualizar interface Appointment em types.ts</name>
  <files>types.ts</files>
  <read_first>
    - types.ts (ler o arquivo completo antes de modificar — estado atual da interface Appointment)
  </read_first>
  <action>
Abrir `types.ts` e localizar a interface `Appointment` (linha ~26). Adicionar os campos novos após o campo `notes?: string`:

```typescript
export interface Appointment {
  id: string;
  clientName: string;
  client_id?: string;
  clientPhone?: string;
  service: string;
  time: string;
  appointment_time: string;
  status: 'Confirmed' | 'Pending' | 'Completed';
  price: number;
  basePrice?: number;
  professional_id?: string;
  notes?: string;
  user_id?: string;
  payment_method?: string;        // 'pix' | 'cash' | 'debit' | 'credit' | null — já existe no banco (20260218)
  received_by?: string | null;    // UUID → team_members.id — quem recebeu o pagamento
  machine_fee_applied?: boolean;  // se taxa de maquininha foi aplicada
  machine_fee_percent?: number | null; // percentual da taxa aplicada
}
```

ATENÇÃO: `payment_method` pode já existir no arquivo (verificado como `payment_method?: string` em types.ts linha 145 em outro contexto — confirmar ao ler o arquivo). Não duplicar. Adicionar apenas os campos que não existem ainda.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "received_by" types.ts` retorna `received_by?: string | null;`
    - `grep "machine_fee_applied" types.ts` retorna `machine_fee_applied?: boolean;`
    - `grep "machine_fee_percent" types.ts` retorna `machine_fee_percent?: number | null;`
    - `npm run typecheck` termina sem erros de tipo relacionados a Appointment
  </acceptance_criteria>
  <done>Interface Appointment atualizada com received_by, machine_fee_applied, machine_fee_percent sem quebrar typecheck.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>[BLOCKING] Tarefa 3: Aplicar schema no Supabase</name>
  <read_first>
    - supabase/migrations/20260413_checkout_fields.sql (confirmar arquivo criado na Tarefa 1)
  </read_first>
  <action>
Executar o push do schema no Supabase para aplicar a migration recém-criada.

Rodar o comando:
```bash
supabase db push
```

Se o comando solicitar confirmação interativa (ex: "Do you want to push these changes?"), confirmar com "y".

Se `supabase` CLI não estiver configurado localmente, aplicar o SQL manualmente no Supabase Dashboard:
1. Ir em SQL Editor no Supabase Dashboard
2. Colar o conteúdo de `supabase/migrations/20260413_checkout_fields.sql`
3. Executar
  </action>
  <what-built>Migration 20260413_checkout_fields.sql com 6 novos campos (3 em appointments, 3 em business_settings)</what-built>
  <how-to-verify>
    Após executar o push, verificar no Supabase Dashboard (Table Editor):
    1. Abrir tabela `appointments` → confirmar colunas `received_by`, `machine_fee_applied`, `machine_fee_percent` existem
    2. Abrir tabela `business_settings` → confirmar colunas `machine_fee_enabled`, `debit_fee_percent`, `credit_fee_percent` existem

    Alternativa CLI: `supabase db diff` deve retornar sem alterações pendentes após o push.
  </how-to-verify>
  <resume-signal>Digite "schema aplicado" após confirmar que as 6 colunas existem nas tabelas, ou descreva o erro encontrado.</resume-signal>
  <acceptance_criteria>
    - Tabela `appointments` tem coluna `received_by` do tipo UUID
    - Tabela `appointments` tem coluna `machine_fee_applied` do tipo BOOLEAN com DEFAULT FALSE
    - Tabela `appointments` tem coluna `machine_fee_percent` do tipo DECIMAL(5,2)
    - Tabela `business_settings` tem coluna `machine_fee_enabled` do tipo BOOLEAN com DEFAULT FALSE
    - Tabela `business_settings` tem coluna `debit_fee_percent` do tipo DECIMAL(5,2) com DEFAULT 0
    - Tabela `business_settings` tem coluna `credit_fee_percent` do tipo DECIMAL(5,2) com DEFAULT 0
  </acceptance_criteria>
  <done>Schema aplicado no Supabase com as 6 novas colunas verificadas via Dashboard ou CLI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| cliente SQL → banco Supabase | Migration aplicada com privilégio de service role; colunas adicionadas sem alterar RLS existente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | appointments.received_by FK | mitigate | `ON DELETE SET NULL` — se team_member for deletado, received_by vira NULL sem quebrar o appointment |
| T-03-02 | Tampering | business_settings novos campos | accept | RLS UPDATE com `get_auth_role() = 'owner'` já ativo — staff não pode alterar taxas |
| T-03-03 | Information Disclosure | migration exposta em repo público | accept | Arquivo contém apenas estrutura DDL, sem dados sensíveis ou credenciais |
</threat_model>

<verification>
Após conclusão do plano:
1. `grep -c "ADD COLUMN IF NOT EXISTS" supabase/migrations/20260413_checkout_fields.sql` retorna `6`
2. `grep "received_by" types.ts` retorna linha com `received_by?: string | null;`
3. `npm run typecheck` passa sem erros
4. Schema confirmado no Supabase Dashboard com 6 novas colunas
</verification>

<success_criteria>
- Migration SQL criada com `IF NOT EXISTS` em todos os ADD COLUMN
- Interface Appointment em types.ts atualizada com 3 novos campos
- Schema aplicado no Supabase sem erros
- `npm run typecheck` passa
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-01-SUMMARY.md` com:
- Arquivos criados/modificados
- Confirmação das 6 colunas aplicadas
- Qualquer desvio do plano
</output>
