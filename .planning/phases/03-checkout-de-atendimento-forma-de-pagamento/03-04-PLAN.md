---
phase: 03-checkout-de-atendimento-forma-de-pagamento
plan: 04
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
files_modified:
  - pages/Agenda.tsx
autonomous: true
requirements:
  - REQ-020
  - REQ-021
  - REQ-022
  - REQ-023

must_haves:
  truths:
    - "Clicar no botão Concluir abre CheckoutModal — NÃO chama handleCompleteAppointment diretamente"
    - "Agendamento concluído exibe badge 'Pago via [método]' e botão Concluir desaparece"
    - "Colaborador (role=staff) NÃO pode cancelar agendamento com status Completed — mensagem: 'Este agendamento já foi finalizado. Fale com o dono.'"
    - "Campo payment_method é OPCIONAL na criação de agendamento (validação removida)"
    - "CheckoutModal recebe teamMembers e financialSettings carregados da empresa"
    - "Após confirmar no modal, Agenda recarrega os agendamentos"
  artifacts:
    - path: "pages/Agenda.tsx"
      provides: "Fluxo de checkout integrado com CheckoutModal"
      contains: "checkoutAppointment"
  key_links:
    - from: "botão Concluir (linha ~1578)"
      to: "setCheckoutAppointment(apt)"
      via: "substituição de handleCompleteAppointment(apt.id)"
      pattern: "setCheckoutAppointment"
    - from: "handleCancelAppointment"
      to: "showAlert com mensagem de bloqueio"
      via: "verificação apt.status === 'Completed' && role === 'staff'"
      pattern: "Este agendamento já foi finalizado"
---

<objective>
Integrar o `CheckoutModal` em `Agenda.tsx`: interceptar o botão "Concluir", carregar os dados necessários (teamMembers, financialSettings), renderizar o modal, exibir badge em agendamentos concluídos, e bloquear cancelamento de agendamentos finalizados por colaboradores.

Purpose: Une todos os planos anteriores em um fluxo coerente — o usuário consegue concluir um atendimento com pagamento registrado, e o sistema protege contra ações inválidas post-conclusão.
Output: Fluxo de checkout completo funcionando em /#/agenda.
</objective>

<execution_context>
@C:/Users/User/Downloads/Rhian-Lepore-main/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/Downloads/Rhian-Lepore-main/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-CONTEXT.md
@.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-RESEARCH.md
@.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-02-SUMMARY.md
@.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-03-SUMMARY.md

<interfaces>
<!-- CheckoutModal — criado no Plano 02 -->
```typescript
export interface CheckoutModalProps {
  appointment: Appointment | null;
  teamMembers: Array<{ id: string; name: string; active: boolean }>;
  financialSettings: {
    machine_fee_enabled: boolean;
    debit_fee_percent: number;
    credit_fee_percent: number;
  } | null;
  onClose: () => void;
  onConfirm: () => void;
}
// Uso: <CheckoutModal appointment={checkoutAppointment} teamMembers={...} financialSettings={...} onClose={() => setCheckoutAppointment(null)} onConfirm={handleCheckoutConfirmed} />
```

<!-- Estado existente em Agenda.tsx — padrão verificado -->
```typescript
// Agenda.tsx já usa padrão:
const [editingAppointment, setEditingAppointment] = useState<Appointment | null>(null);
// Replicar para checkout:
const [checkoutAppointment, setCheckoutAppointment] = useState<Appointment | null>(null);
```

<!-- Botão Concluir — linha 1578 em Agenda.tsx -->
```typescript
// ANTES:
<button onClick={() => handleCompleteAppointment(apt.id)} className="..." title="Concluir">
  <Check className="w-3.5 h-3.5 text-green-500" />
</button>

// DEPOIS (per D-02 e RESEARCH Pattern 1):
{!isCompleted && (
  <button onClick={() => setCheckoutAppointment(apt)} className="..." title="Concluir">
    <Check className="w-3.5 h-3.5 text-green-500" />
  </button>
)}
```

<!-- handleCancelAppointment — linha 834 em Agenda.tsx -->
```typescript
// ANTES:
const handleCancelAppointment = async (appointmentId: string, isOverdue: boolean = false) => {
  if (!confirm('Cancelar este agendamento? ...')) return;
  // ...

// DEPOIS — adicionar guard no início (per D-07):
const handleCancelAppointment = async (appointmentId: string, isOverdue: boolean = false) => {
  const apt = appointments.find(a => a.id === appointmentId)
    || overdueAppointments.find(a => a.id === appointmentId);
  if (apt?.status === 'Completed' && role === 'staff') {
    showAlert({
      type: 'error',
      message: 'Este agendamento já foi finalizado. Fale com o dono.'
    });
    return;
  }
  if (!confirm('Cancelar este agendamento? ...')) return;
  // ... resto igual
```

<!-- Badge para agendamentos concluídos — padrão verificado Agenda.tsx:1661 -->
```typescript
// Adicionar após o bloco de botões, quando isCompleted:
{isCompleted && apt.payment_method && (
  <span className="text-xs font-mono bg-green-500/10 text-green-400 px-2 py-1 rounded">
    Pago via {apt.payment_method.toUpperCase()}
  </span>
)}
```

<!-- role do useAuth em Agenda.tsx -->
```typescript
const { user, companyId, role } = useAuth();
// role: 'owner' | 'staff'
```

<!-- Busca de financialSettings no fetchData de Agenda.tsx -->
```typescript
// Adicionar ao fetchData existente ou criar fetchFinancialSettings separado
const { data: finSettings } = await supabase
  .from('business_settings')
  .select('machine_fee_enabled, debit_fee_percent, credit_fee_percent')
  .eq('user_id', companyId)   // companyId, nunca user.id
  .maybeSingle();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Tarefa 1: Adicionar estado, busca de dados e integração do CheckoutModal em Agenda.tsx</name>
  <files>pages/Agenda.tsx</files>
  <read_first>
    - pages/Agenda.tsx linhas 1-100 (imports e início do componente — verificar o que já está importado)
    - pages/Agenda.tsx linhas 100-200 (useState declarations — para adicionar novos estados no padrão correto)
    - pages/Agenda.tsx linhas 200-400 (fetchData — para adicionar busca de financialSettings e teamMembers para checkout)
    - pages/Agenda.tsx linhas 743-855 (handleCompleteAppointment e handleCancelAppointment — para modificar)
    - pages/Agenda.tsx linhas 1530-1590 (card de agendamento com botão Concluir — para modificar)
    - pages/Agenda.tsx linhas 1640-1680 (seção de modais no final do JSX — para adicionar CheckoutModal)
    - components/CheckoutModal.tsx (confirmar exports e props antes de importar)
  </read_first>
  <action>
Fazer as seguintes modificações em `pages/Agenda.tsx`. Ler CADA seção antes de modificar.

**Modificação 1 — Adicionar import do CheckoutModal** (após os outros imports de componentes):
```typescript
import { CheckoutModal } from '../components/CheckoutModal';
```

**Modificação 2 — Adicionar estados para checkout** (no bloco de useState, após `editingAppointment`):
```typescript
const [checkoutAppointment, setCheckoutAppointment] = useState<Appointment | null>(null);
const [checkoutTeamMembers, setCheckoutTeamMembers] = useState<Array<{ id: string; name: string; active: boolean }>>([]);
const [financialSettings, setFinancialSettings] = useState<{
  machine_fee_enabled: boolean;
  debit_fee_percent: number;
  credit_fee_percent: number;
} | null>(null);
```

**Modificação 3 — Adicionar busca de dados para checkout** (dentro do fetchData existente, após as queries principais, OU criar função `fetchCheckoutData` chamada no useEffect junto com fetchData):

```typescript
// Buscar team members para dropdown "Recebido por"
const { data: membersData } = await supabase
  .from('team_members')
  .select('id, name, active')
  .eq('user_id', companyId)   // companyId do useAuth(), não user.id
  .eq('active', true)
  .order('name');
setCheckoutTeamMembers(membersData || []);

// Buscar configurações financeiras para pré-preencher taxa
const { data: finSettings } = await supabase
  .from('business_settings')
  .select('machine_fee_enabled, debit_fee_percent, credit_fee_percent')
  .eq('user_id', companyId)   // companyId, não user.id — pega settings do dono
  .maybeSingle();
setFinancialSettings(finSettings);
```

**Modificação 4 — handleCancelAppointment: adicionar guard de agendamento concluído** (INÍCIO da função, antes do confirm):

```typescript
const handleCancelAppointment = async (appointmentId: string, isOverdue: boolean = false) => {
  // Guard: staff não pode cancelar agendamento já finalizado (D-07)
  const apt = appointments.find((a) => a.id === appointmentId)
    || overdueAppointments.find((a) => a.id === appointmentId);
  if (apt?.status === 'Completed' && role === 'staff') {
    showAlert({
      type: 'error',
      message: 'Este agendamento já foi finalizado. Fale com o dono.'
    });
    return;
  }
  // ... resto do código existente (confirm, update, etc.) — NÃO alterar
```

ATENÇÃO: Verificar se `showAlert` já está sendo usado em Agenda.tsx. Se não, adicionar `const { showAlert } = useAlerts();` e importar `useAlerts` de `'../contexts/AlertsContext'`.

**Modificação 5 — Botão Concluir no card** (linha ~1578): substituir chamada direta por `setCheckoutAppointment`:

```typescript
// ANTES:
<button onClick={() => handleCompleteAppointment(apt.id)} className="p-1 hover:bg-white/10 rounded transition-colors" title="Concluir">
  <Check className="w-3.5 h-3.5 text-green-500" />
</button>

// DEPOIS:
{!isCompleted && (
  <button
    onClick={() => setCheckoutAppointment(apt)}
    className="p-1 hover:bg-white/10 rounded transition-colors"
    title="Concluir"
  >
    <Check className="w-3.5 h-3.5 text-green-500" />
  </button>
)}
```

NOTA: `isCompleted` já está definido como `const isCompleted = apt.status === 'Completed'` na linha ~1535 — usar essa variável.

**Modificação 6 — Badge "Pago via [método]"** (adicionar após o bloco `isCompleted ? ...` de status, dentro do card):

```typescript
{isCompleted && apt.payment_method && (
  <span className="text-xs font-mono bg-green-500/10 text-green-400 px-2 py-1 rounded mt-1 inline-block">
    Pago via {apt.payment_method.toUpperCase()}
  </span>
)}
```

**Modificação 7 — Renderizar CheckoutModal** (no final do JSX, junto com os outros modais):

```typescript
{/* CheckoutModal — Plano 03 Fase 3 */}
<CheckoutModal
  appointment={checkoutAppointment}
  teamMembers={checkoutTeamMembers}
  financialSettings={financialSettings}
  onClose={() => setCheckoutAppointment(null)}
  onConfirm={() => {
    setCheckoutAppointment(null);
    fetchData();
  }}
/>
```
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | grep -E "error|warning" | grep -i "Agenda" | head -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "checkoutAppointment" pages/Agenda.tsx` retorna ao menos 3 ocorrências (useState, setCheckoutAppointment no botão, no JSX do modal)
    - `grep "CheckoutModal" pages/Agenda.tsx` retorna ao menos 2 ocorrências (import + uso JSX)
    - `grep "Este agendamento já foi finalizado" pages/Agenda.tsx` retorna resultado
    - `grep "setCheckoutAppointment(apt)" pages/Agenda.tsx` retorna resultado (botão Concluir modificado)
    - `grep "Pago via" pages/Agenda.tsx` retorna resultado (badge)
    - `grep "checkoutTeamMembers\|financialSettings" pages/Agenda.tsx` retorna resultados (estados declarados)
    - `npm run typecheck` passa sem erros em Agenda.tsx
    - `npm run lint` passa sem warnings em Agenda.tsx
  </acceptance_criteria>
  <done>Agenda.tsx integrada com CheckoutModal. Botão Concluir abre modal, badge exibido para pagamentos concluídos, colaboradores bloqueados de cancelar atendimentos finalizados.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Tornar payment_method opcional na criação de agendamento</name>
  <files>pages/Agenda.tsx</files>
  <read_first>
    - pages/Agenda.tsx (buscar por "payment_method" com grep — encontrar onde é validado como obrigatório na criação)
    - components/AppointmentWizard.tsx linhas 1-50 (verificar se validação de payment_method existe lá também)
  </read_first>
  <action>
Buscar no código de CRIAÇÃO de agendamento (não no handleCompleteAppointment) qualquer validação que force `payment_method` obrigatório. Per D-01: campo agora é OPCIONAL na criação.

**Passo 1:** No terminal, executar para encontrar usos:
```bash
grep -n "payment_method" pages/Agenda.tsx
grep -n "payment_method" components/AppointmentWizard.tsx
```

**Passo 2:** Identificar validações de obrigatoriedade. Padrão a procurar:
- `if (!paymentMethod)` ou `paymentMethod === ''` no fluxo de criação
- `required` em campos de payment_method em formulários de novo agendamento
- Validação no submit do wizard que bloqueia sem payment_method

**Passo 3:** Remover ou tornar opcional:
- Se campo de payment_method tiver `required` no HTML → remover o atributo `required`
- Se houver validação JS que bloqueia sem payment_method no fluxo de CRIAÇÃO → remover ou comentar com `// Removido em Fase 3 — D-01: payment_method agora capturado no checkout`
- Se o campo existir como select/radio na criação → manter o campo mas remover obrigatoriedade (pode ser deixado em branco)

ATENÇÃO: NÃO remover validação do handleCompleteAppointment ou do CheckoutModal — lá ainda é obrigatório.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck` passa sem erros
    - `npm run lint` passa sem warnings
    - `npm test -- --run 2>&1 | tail -10` não apresenta novos test failures além dos já existentes antes desta fase
    - Executar `npm run build` → build completa sem erros
  </acceptance_criteria>
  <done>payment_method removido como campo obrigatório na criação de agendamento. Build e typecheck limpos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| botão Concluir → CheckoutModal | Appointment passado como prop — não executa operação diretamente |
| handleCancelAppointment | Verifica role e status antes de executar UPDATE |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-12 | Elevation of Privilege | handleCancelAppointment | mitigate | Guard `apt.status === 'Completed' && role === 'staff'` bloqueado na UI + RLS no banco como segunda linha de defesa |
| T-03-13 | Tampering | checkoutAppointment state | accept | Usuário só pode definir checkoutAppointment ao clicar em agendamento da própria empresa (já filtrado pelo fetchData com companyId) |
| T-03-14 | Spoofing | apt passado para CheckoutModal | accept | apt vem do state local preenchido via fetchData com company_id do auth — não aceita input de URL/form |
</threat_model>

<verification>
Após conclusão do plano (verificação final de toda a Fase 3):
1. `npm test -- --run 2>&1 | tail -15` → todos os testes passando (incluindo CheckoutModal e FinancialSettings)
2. `npm run typecheck` → sem erros
3. `npm run lint` → sem warnings
4. `npm run build` → build bem-sucedido
5. Verificação manual: `npm run dev` → acessar /#/agenda → clicar Concluir em agendamento → CheckoutModal abre com campos corretos
6. Verificação manual: acessar /#/configuracoes/financeiro → página de taxas disponível para owner
</verification>

<success_criteria>
- Fluxo completo funcional: botão Concluir → CheckoutModal → confirmar → agendamento marcado como concluído + badge exibido
- Colaborador não consegue cancelar agendamento concluído (mensagem de erro)
- payment_method opcional na criação
- Build e testes passando
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-04-SUMMARY.md` com:
- Lista de todas as modificações em Agenda.tsx
- Resultado do build final
- Resultado da suite completa de testes
- Confirmação dos 5 critérios de sucesso acima
</output>
