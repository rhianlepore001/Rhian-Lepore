---
phase: 03-checkout-de-atendimento-forma-de-pagamento
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - pages/settings/FinancialSettings.tsx
  - pages/settings/FinancialSettings.test.tsx
  - constants.ts
  - App.tsx
autonomous: true
requirements:
  - REQ-024

must_haves:
  truths:
    - "Settings > Financeiro acessível em /#/configuracoes/financeiro apenas para owner"
    - "Toggle 'Repassar taxa ao colaborador' salva em business_settings.machine_fee_enabled"
    - "Campos de % débito e crédito aparecem apenas quando toggle está ativo"
    - "Campos salvos via upsert com onConflict: 'user_id' em business_settings"
    - "Rota protegida com OwnerRouteGuard — staff redirecionado"
    - "Entrada 'Financeiro' aparece no menu lateral de Settings"
  artifacts:
    - path: "pages/settings/FinancialSettings.tsx"
      provides: "Página de configurações financeiras (toggle taxa + percentuais)"
      exports: ["FinancialSettings"]
      min_lines: 80
    - path: "constants.ts"
      provides: "Entrada 'Financeiro' em SETTINGS_ITEMS"
      contains: "configuracoes/financeiro"
    - path: "App.tsx"
      provides: "Rota lazy /configuracoes/financeiro com OwnerRouteGuard"
      contains: "FinancialSettings"
  key_links:
    - from: "FinancialSettings handleSave"
      to: "business_settings upsert"
      via: "supabase.from('business_settings').upsert({ user_id: user.id, machine_fee_enabled, debit_fee_percent, credit_fee_percent }, { onConflict: 'user_id' })"
      pattern: "upsert.*machine_fee_enabled"
    - from: "App.tsx"
      to: "FinancialSettings"
      via: "React.lazy import + OwnerRouteGuard"
      pattern: "FinancialSettings.*OwnerRouteGuard"
---

<objective>
Criar a página `FinancialSettings.tsx`, registrar a entrada no menu lateral de Settings em `constants.ts`, e adicionar a rota lazy com `OwnerRouteGuard` em `App.tsx` — entregando REQ-024 completo.

Purpose: Permite que o dono configure taxas de maquininha uma vez; essas configurações são carregadas pelo CheckoutModal para pré-preencher o campo de taxa.
Output: Página funcional em /#/configuracoes/financeiro acessível apenas para owner.
</objective>

<execution_context>
@C:/Users/User/Downloads/Rhian-Lepore-main/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/User/Downloads/Rhian-Lepore-main/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-CONTEXT.md
@.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-RESEARCH.md

<interfaces>
<!-- Padrão de settings — CommissionsSettings.tsx (referência estrutural) -->
```typescript
// Padrão de busca de business_settings (CommissionsSettings.tsx:61-70)
const { data: settingsData } = await supabase
  .from('business_settings')
  .select('commission_settlement_day_of_month')
  .eq('user_id', user.id)
  .single();

// Padrão de upsert (CommissionsSettings handleSaveSettlementDay)
await supabase
  .from('business_settings')
  .upsert({
    user_id: user.id,
    campo: valor,
    updated_at: new Date().toISOString(),
  }, { onConflict: 'user_id' });
```

<!-- Padrão de SETTINGS_ITEMS em constants.ts (atual) -->
```typescript
export const SETTINGS_ITEMS = [
  { label: 'Geral', path: '/configuracoes/geral', icon: Settings },
  { label: 'Agendamento', path: '/configuracoes/agendamento', icon: Calendar },
  { label: 'Equipe', path: '/configuracoes/equipe', icon: Users },
  { label: 'Serviços', path: '/configuracoes/servicos', icon: Package },
  { label: 'Comissões', path: '/configuracoes/comissoes', icon: DollarSign },
  { label: 'Assinatura', path: '/configuracoes/assinatura', icon: CreditCard },
  // ... mais itens
];
// ADICIONAR APÓS 'Comissões':
// { label: 'Financeiro', path: '/configuracoes/financeiro', icon: DollarSign },
// NOTA: DollarSign já está importado em constants.ts
```

<!-- Padrão de rota lazy em App.tsx -->
```typescript
// Importação lazy (linhas 22-31 do App.tsx):
const CommissionsSettings = React.lazy(() =>
  import('./pages/settings/CommissionsSettings').then(module => ({ default: module.CommissionsSettings }))
);

// Rota com OwnerRouteGuard (linha 160):
<Route path="/configuracoes/comissoes" element={<OwnerRouteGuard><CommissionsSettings /></OwnerRouteGuard>} />
```

<!-- Padrão de SettingsLayout + BrutalCard -->
```typescript
import { SettingsLayout } from '../../components/SettingsLayout';
import { BrutalCard } from '../../components/BrutalCard';
// SettingsLayout encapsula toda a página de settings com sidebar
// BrutalCard: wrapper com borda e padding para seções
```

<!-- useAuth hook -->
```typescript
const { user, userType } = useAuth();
// user.id = ID do owner (para upsert em business_settings)
// userType: 'barber' | 'beauty' (para tema visual)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar FinancialSettings.tsx com testes</name>
  <files>pages/settings/FinancialSettings.tsx, pages/settings/FinancialSettings.test.tsx</files>
  <read_first>
    - pages/settings/CommissionsSettings.tsx (ler completo — referência estrutural para padrão de settings)
    - components/SettingsLayout.tsx (verificar interface de props)
    - components/BrutalCard.tsx (verificar interface de props)
    - components/BrutalButton.tsx (props loading, variant)
  </read_first>
  <behavior>
    - Testes (criar FinancialSettings.test.tsx primeiro):
      - Teste 1: renderiza sem crash para owner
      - Teste 2: campos de % não aparecem quando toggle está desativado
      - Teste 3: campos de % aparecem quando toggle é ativado
      - Teste 4: botão Salvar chama supabase.upsert com dados corretos
    - Implementação (FinancialSettings.tsx):
      - Usa SettingsLayout + BrutalCard como wrapper
      - Carrega business_settings ao montar usando `.eq('user_id', user.id)`
      - Toggle "Repassar taxa de maquininha ao colaborador?"
      - Campos de taxa débito e crédito aparecem SOMENTE quando toggle ativo
      - Botão Salvar com loading state
      - upsert com `onConflict: 'user_id'` em business_settings
      - showAlert em sucesso e erro
  </behavior>
  <action>
Criar primeiro `pages/settings/FinancialSettings.test.tsx`:

```typescript
import { render, screen, fireEvent, waitFor } from '@testing-library/react';
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { FinancialSettings } from './FinancialSettings';

vi.mock('../../lib/supabase', () => ({
  supabase: {
    from: vi.fn().mockReturnThis(),
    select: vi.fn().mockReturnThis(),
    upsert: vi.fn().mockResolvedValue({ data: null, error: null }),
    eq: vi.fn().mockReturnThis(),
    single: vi.fn().mockResolvedValue({ data: null, error: null }),
  }
}));

vi.mock('../../contexts/AuthContext', () => ({
  useAuth: () => ({
    user: { id: 'owner-123' },
    userType: 'barber',
  })
}));

vi.mock('../../contexts/AlertsContext', () => ({
  useAlerts: () => ({ showAlert: vi.fn() })
}));

vi.mock('../../components/SettingsLayout', () => ({
  SettingsLayout: ({ children }: { children: React.ReactNode }) => <div>{children}</div>
}));

describe('FinancialSettings', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('renderiza sem crash', () => {
    render(<FinancialSettings />);
    expect(screen.getByText(/repassar taxa/i)).toBeInTheDocument();
  });

  it('campos de porcentagem NÃO aparecem quando toggle desativado', () => {
    render(<FinancialSettings />);
    expect(screen.queryByLabelText(/taxa débito/i)).not.toBeInTheDocument();
    expect(screen.queryByLabelText(/taxa crédito/i)).not.toBeInTheDocument();
  });

  it('campos de porcentagem APARECEM quando toggle ativado', () => {
    render(<FinancialSettings />);
    const toggle = screen.getByRole('checkbox', { name: /repassar taxa/i });
    fireEvent.click(toggle);
    expect(screen.getByLabelText(/taxa débito/i)).toBeInTheDocument();
    expect(screen.getByLabelText(/taxa crédito/i)).toBeInTheDocument();
  });

  it('botão Salvar aciona upsert em business_settings', async () => {
    const { supabase } = await import('../../lib/supabase');
    render(<FinancialSettings />);
    fireEvent.click(screen.getByText(/salvar/i));
    await waitFor(() => {
      expect(supabase.from).toHaveBeenCalledWith('business_settings');
    });
  });
});
```

Depois criar `pages/settings/FinancialSettings.tsx`:

```typescript
import React, { useState, useEffect } from 'react';
import { supabase } from '../../lib/supabase';
import { useAuth } from '../../contexts/AuthContext';
import { useAlerts } from '../../contexts/AlertsContext';
import { BrutalCard } from '../../components/BrutalCard';
import { BrutalButton } from '../../components/BrutalButton';
import { SettingsLayout } from '../../components/SettingsLayout';
import { DollarSign, Loader2 } from 'lucide-react';
import { logger } from '../../utils/Logger';

export const FinancialSettings: React.FC = () => {
  const { user, userType } = useAuth();
  const { showAlert } = useAlerts();

  const isBeauty = userType === 'beauty';
  const accentText = isBeauty ? 'text-beauty-neon' : 'text-accent-gold';

  const [loading, setLoading] = useState(true);
  const [saving, setSaving] = useState(false);
  const [machineFeeEnabled, setMachineFeeEnabled] = useState(false);
  const [debitFee, setDebitFee] = useState<string>('');
  const [creditFee, setCreditFee] = useState<string>('');

  useEffect(() => {
    fetchSettings();
  }, [user]);

  const fetchSettings = async () => {
    if (!user) return;
    setLoading(true);
    try {
      const { data, error } = await supabase
        .from('business_settings')
        .select('machine_fee_enabled, debit_fee_percent, credit_fee_percent')
        .eq('user_id', user.id)
        .single();

      if (!error && data) {
        setMachineFeeEnabled(data.machine_fee_enabled ?? false);
        setDebitFee(data.debit_fee_percent?.toString() ?? '');
        setCreditFee(data.credit_fee_percent?.toString() ?? '');
      }
    } catch (err) {
      logger.error('Erro ao carregar configurações financeiras', err);
    } finally {
      setLoading(false);
    }
  };

  const handleSave = async () => {
    if (!user) return;
    setSaving(true);
    try {
      const { error } = await supabase
        .from('business_settings')
        .upsert({
          user_id: user.id,
          machine_fee_enabled: machineFeeEnabled,
          debit_fee_percent: machineFeeEnabled ? parseFloat(debitFee) || 0 : 0,
          credit_fee_percent: machineFeeEnabled ? parseFloat(creditFee) || 0 : 0,
          updated_at: new Date().toISOString(),
        }, { onConflict: 'user_id' });

      if (error) throw error;

      showAlert({ type: 'success', message: 'Configurações financeiras salvas!' });
    } catch (err) {
      logger.error('Erro ao salvar configurações financeiras', err);
      showAlert({ type: 'error', message: 'Erro ao salvar. Tente novamente.' });
    } finally {
      setSaving(false);
    }
  };

  if (loading) {
    return (
      <SettingsLayout>
        <div className="flex items-center justify-center py-12">
          <Loader2 className="w-6 h-6 animate-spin text-neutral-400" />
        </div>
      </SettingsLayout>
    );
  }

  return (
    <SettingsLayout>
      <div className="space-y-6">
        <div>
          <h2 className={`text-xl font-bold font-mono ${accentText}`}>
            Configurações Financeiras
          </h2>
          <p className="text-neutral-400 text-sm mt-1">
            Configure o repasse de taxa de maquininha para os colaboradores.
          </p>
        </div>

        <BrutalCard>
          <div className="space-y-4">
            <div className="flex items-center justify-between">
              <div>
                <p className="font-medium text-white">Repassar taxa de maquininha ao colaborador?</p>
                <p className="text-xs text-neutral-400 mt-0.5">
                  Se ativo, a comissão é calculada sobre o valor já descontado da taxa.
                </p>
              </div>
              <label className="relative inline-flex items-center cursor-pointer">
                <input
                  type="checkbox"
                  role="checkbox"
                  aria-label="Repassar taxa de maquininha ao colaborador?"
                  checked={machineFeeEnabled}
                  onChange={(e) => setMachineFeeEnabled(e.target.checked)}
                  className="sr-only peer"
                />
                <div className="w-11 h-6 bg-neutral-700 peer-focus:outline-none rounded-full peer peer-checked:after:translate-x-full peer-checked:after:border-white after:content-[''] after:absolute after:top-[2px] after:left-[2px] after:bg-white after:border-gray-300 after:border after:rounded-full after:h-5 after:w-5 after:transition-all peer-checked:bg-accent-gold"></div>
              </label>
            </div>

            {machineFeeEnabled && (
              <div className="grid grid-cols-2 gap-4 pt-2 border-t border-neutral-700">
                <div>
                  <label htmlFor="debit-fee" className="block text-xs font-mono uppercase text-neutral-400 mb-1">
                    Taxa Débito (%)
                  </label>
                  <input
                    id="debit-fee"
                    aria-label="Taxa débito (%)"
                    type="number"
                    step="0.01"
                    min="0"
                    max="100"
                    placeholder="Ex: 2.5"
                    value={debitFee}
                    onChange={(e) => setDebitFee(e.target.value)}
                    className="w-full bg-neutral-800 border border-neutral-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-accent-gold"
                  />
                </div>
                <div>
                  <label htmlFor="credit-fee" className="block text-xs font-mono uppercase text-neutral-400 mb-1">
                    Taxa Crédito (%)
                  </label>
                  <input
                    id="credit-fee"
                    aria-label="Taxa crédito (%)"
                    type="number"
                    step="0.01"
                    min="0"
                    max="100"
                    placeholder="Ex: 3.5"
                    value={creditFee}
                    onChange={(e) => setCreditFee(e.target.value)}
                    className="w-full bg-neutral-800 border border-neutral-700 rounded-lg px-3 py-2 text-white focus:outline-none focus:border-accent-gold"
                  />
                </div>
              </div>
            )}
          </div>
        </BrutalCard>

        <div className="flex justify-end">
          <BrutalButton variant="primary" onClick={handleSave} loading={saving}>
            Salvar Configurações
          </BrutalButton>
        </div>
      </div>
    </SettingsLayout>
  );
};
```

Após criar ambos os arquivos, executar:
```bash
npm test -- --run --reporter=verbose pages/settings/FinancialSettings.test
```
  </action>
  <verify>
    <automated>npm test -- --run --reporter=verbose pages/settings/FinancialSettings.test 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run pages/settings/FinancialSettings.test` reporta 4/4 testes passando
    - `grep "machine_fee_enabled" pages/settings/FinancialSettings.tsx` retorna resultado
    - `grep "onConflict.*user_id" pages/settings/FinancialSettings.tsx` retorna resultado
    - `grep "OwnerRouteGuard\|owner" pages/settings/FinancialSettings.tsx` NÃO precisa existir (proteção fica em App.tsx)
    - `npm run typecheck` passa sem erros em FinancialSettings.tsx
  </acceptance_criteria>
  <done>FinancialSettings implementado com toggle + campos de taxa + upsert. 4/4 testes passando.</done>
</task>

<task type="auto">
  <name>Tarefa 2: Registrar rota e entrada no menu de Settings</name>
  <files>constants.ts, App.tsx</files>
  <read_first>
    - constants.ts (ler completo — ver SETTINGS_ITEMS atual antes de modificar)
    - App.tsx (ler linhas 1-50 e 140-170 — padrão de imports lazy e rotas de settings)
  </read_first>
  <action>
**constants.ts:** Adicionar entrada `Financeiro` em SETTINGS_ITEMS após a entrada `Comissões`:

```typescript
// ANTES (linha ~21):
{ label: 'Comissões', path: '/configuracoes/comissoes', icon: DollarSign },

// DEPOIS (adicionar a linha abaixo de Comissões):
{ label: 'Comissões', path: '/configuracoes/comissoes', icon: DollarSign },
{ label: 'Financeiro', path: '/configuracoes/financeiro', icon: DollarSign },
```

NOTA: `DollarSign` já está importado no topo do arquivo — não adicionar import duplicado.

---

**App.tsx — parte 1: Adicionar import lazy** após a linha de `CommissionsSettings`:

```typescript
// Após linha ~26 (CommissionsSettings lazy):
const FinancialSettings = React.lazy(() =>
  import('./pages/settings/FinancialSettings').then(module => ({ default: module.FinancialSettings }))
);
```

**App.tsx — parte 2: Adicionar rota** após a rota de comissões (linha ~160):

```typescript
// Após:
<Route path="/configuracoes/comissoes" element={<OwnerRouteGuard><CommissionsSettings /></OwnerRouteGuard>} />
// Adicionar:
<Route path="/configuracoes/financeiro" element={<OwnerRouteGuard><FinancialSettings /></OwnerRouteGuard>} />
```

A rota DEVE estar dentro de `<OwnerRouteGuard>` — per D-06 e Pitfall 6 do RESEARCH.md.
  </action>
  <verify>
    <automated>npm run typecheck 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "configuracoes/financeiro" constants.ts` retorna `{ label: 'Financeiro', path: '/configuracoes/financeiro', icon: DollarSign }`
    - `grep "FinancialSettings" App.tsx` retorna 2 resultados (import lazy + rota)
    - `grep "OwnerRouteGuard.*FinancialSettings\|FinancialSettings.*OwnerRouteGuard" App.tsx` retorna resultado
    - `npm run typecheck` passa sem erros
    - `npm run lint` passa sem warnings
  </acceptance_criteria>
  <done>Rota /configuracoes/financeiro registrada em App.tsx com OwnerRouteGuard e entrada 'Financeiro' adicionada em SETTINGS_ITEMS.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Descrição |
|----------|-----------|
| owner UI → business_settings UPDATE | Taxas enviadas via form — validadas pelo banco (DECIMAL constraint) |
| rota /configuracoes/financeiro | Protegida por OwnerRouteGuard no cliente + RLS UPDATE no banco |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Elevation of Privilege | /configuracoes/financeiro (rota) | mitigate | `OwnerRouteGuard` bloqueia staff na UI; RLS com `get_auth_role() = 'owner'` bloqueia no banco |
| T-03-10 | Tampering | debit_fee_percent / credit_fee_percent input | mitigate | `parseFloat()` antes de persistir + `DECIMAL(5,2)` no banco rejeita valores fora do range |
| T-03-11 | Information Disclosure | taxas de maquininha visíveis | accept | Informação não é sensível ao nível de PII; acesso já restrito a owner |
</threat_model>

<verification>
Após conclusão do plano:
1. `npm test -- --run pages/settings/FinancialSettings.test` → 4/4 passando
2. `grep "configuracoes/financeiro" constants.ts` → retorna resultado
3. `grep "OwnerRouteGuard.*FinancialSettings" App.tsx` → retorna resultado
4. `npm run typecheck && npm run lint` → ambos passam sem erros
</verification>

<success_criteria>
- 4 testes passando em FinancialSettings.test.tsx
- Rota /configuracoes/financeiro registrada com OwnerRouteGuard
- Entrada 'Financeiro' em SETTINGS_ITEMS visível no menu lateral
- Typecheck e lint limpos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/03-checkout-de-atendimento-forma-de-pagamento/03-03-SUMMARY.md` com:
- Testes: status 4/4
- Arquivos modificados
- Posição da entrada 'Financeiro' no menu de settings
</output>
